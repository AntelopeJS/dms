---
title: "Deployment"
description: Run the DMS in production — build both processes, start them in the right order, and check off the secrets and multi-instance requirements.
---

# Deployment

Production runs the same two processes as development — the backend and the `ajs dms` frontend ([Architecture](/docs/getting-started/architecture)) — but each is built once and launched from its build artifacts instead of the dev tooling. The steps below are the production-specific ones; the pages linked from each section own the details.

## Build and run the backend

Build the project once, then launch from the artifacts:

```bash [Terminal]
ajs project build
ajs project start
```

`ajs project start` launches from the build output and skips module download and graph validation. `ajs project dev` (and `ajs project run`, its legacy alias) runs the project in dev mode even without `-w` — so don't use either as a production launcher.

Use production values in your `antelope.config.ts`: real secrets (see the [checklist](#production-checklist) below), Redis for realtime, durable file storage, and your SMTP settings. Every key and default is in [Configuration](/docs/building/configuration).

## Build and run the frontend

The frontend is built against a **running backend** — `ajs dms build` downloads the resolved frontend modules from it — then served from the build output:

```bash [Terminal]
export DMS_BOOTSTRAP_SECRET=…   # same value as the backend's frontend.bootstrapSecret
ajs dms build -b https://api.example.com
ajs dms start -b https://api.example.com -p 3001
```

Neither command auto-discovers the backend: pass `-b` or set `DMS_API_BASE_URL`. The URL selects the workspace under `~/.antelopejs/dms-frontend/`, so `start` must receive the same URL as `build`. Flags, workspace behavior, and the `--offline` cached-manifest mode are in [Frontend CLI](/docs/building/frontend-cli).

Set `DMS_SESSION_SECRET` to a stable secret of at least 32 characters on every frontend instance. Keep the complete generated build, including `dist/server/locales/`: the email renderer reads its merged translation catalogs from these JSON files. Copying only the email JavaScript files breaks localized rendering.

`DMS_BOOTSTRAP_SECRET` is what entitles the build host to the layers and their private options, and a backend that configures `frontend.bootstrapSecret` gates them on it: a build host that presents nothing, or the wrong value, gets `401` and `ajs dms build` fails outright rather than producing a frontend that would only break later. That is the intended production shape — it is a missing secret, not a soft degradation.

A backend that configures **no** `frontend.bootstrapSecret` still serves both routes to anyone, minus every private option. The build then succeeds, but the resulting frontend has no `oauth.relaySecret` or `htmlRender.serviceSecret` — provider login and server-side HTML rendering fail at runtime. Both ends say so: `ajs dms build` warns that no layer arrived with private options, and the backend logs the same conclusion once per route. Configure `frontend.bootstrapSecret`. `start` does not need the secret.

Pass it through the environment rather than `--bootstrap-secret`, and note that a credential read from a file keeps its trailing newline — `ajs dms` rejects a value that cannot travel in an HTTP header rather than letting `fetch` fail obscurely.

## Startup order

Start the backend first and wait until it is ready; the frontend asks it for the layers at startup ([Architecture](/docs/getting-started/architecture)). The same ordering applies to the build step: the backend must be up and serving your final module set before you run `ajs dms build`, or the built frontend won't match it.

Rebuild the frontend whenever the layer set changes — you add or remove a module that calls `AddFrontendModule`, or you change a layer's code. Backend-only changes (pages, components, data) need no frontend rebuild: pages are data the frontend draws.

## Production checklist

The defaults are development values. Set each of these before going live:

| Key                                                   | Default   | Production requirement                                                                     |
| ----------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------ |
| `auth.jwtSecret` (DMS config)                         | `""`      | A strong random value — it signs every session token.                                      |
| `htmlRender.serviceSecret` (DMS config)               | `"dev"`   | A strong random value — it signs the render-service tokens.                                |
| `frontend.bootstrapSecret` (DMS config)                   | —         | A strong random value — setting it gates the layer manifest and source archive; unset, both are public. Also set it as `DMS_BOOTSTRAP_SECRET` where you run `ajs dms build`. |
| `secret` (the `@antelopejs/auth-jwt` module's config) | —         | A strong random value, independent of `auth.jwtSecret`.                                    |
| `apiBaseUrl` / `clientBaseUrl`                        | localhost | Your public backend and dashboard URLs — generated links (invite emails, signup) use them. |
| `htmlRender.renderEndpoint`                           | localhost | The production frontend's `/api/html/render` URL, reachable from the backend.              |

All of these live in the DMS `config` block except the auth-jwt secret, which belongs to that module's own entry — [Configuration](/docs/building/configuration) documents each key. Transactional email also needs an `interface-email` implementation (typically `@antelopejs/nodemailer`) wired in the config, or invites, validation, and password resets fail ([Backend services](/docs/building/backend-services)).

## Multiple instances

Running more than one backend instance requires a shared realtime transport:

- **Redis for realtime.** The in-memory driver does not cross process boundaries — events, presence, and notification streams stay on one instance. Load `@antelopejs/redis` and keep `realtime.driver` at `"auto"` or set `"redis"` ([Configuration](/docs/building/configuration#realtime--transport-driver)).

::note
A single-instance deployment works without Redis, but the DMS uses it when present (`realtime.driver: "auto"`), so most production stacks include it from the start.
::
